[probe]
backend = "jlink"
# Explicit decimal USB serial, not index 0/1 or nickname. Keep the real value local.
serial = "REPLACE_WITH_DECIMAL_SERIAL"
executable = "C:/Program Files/SEGGER/JLink/JLinkGDBServerCL.exe"
speed_khz = 1000
flash = "if-different"
