# Host-only fixtures

Frozen target metadata and synthetic contract registry for host tests.
These are not deployment profiles or evidence of reviewed HAL behavior.
The fixture hash identifies no real HAL source. Do not use this registry for HW runs.
Runtime profiles and contracts belong to each consumer project.
